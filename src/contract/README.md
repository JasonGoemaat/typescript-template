# Contracts

You can find contracts by looking at servers like so:

    ns.ls('zer0', '.cct')

Then you can use the contract api to get the contract as a type (15gb for this):

   ns.codingcontract.getContract(filename, 'home')